import Continuity.Codec.Core.Box
import Continuity.Codec.Core.Scanner
import Continuity.Codec.Core.Bytes

set_option autoImplicit false

/- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

      "I knew then that the ghosts were moving through the four stupid
      corners of the room, that I could feel them. The scanner was running
      through its vocabulary, eating the wall of blindness that still
      separated us, pressing the signs of its language into the soft fat
      of the sensorium, word by word, token by token, until meaning
      condensed on the far side of the scan."

                                                                    — Count Zero

    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ -/

namespace Continuity.Codec.Dhall

-- TODO[b7r6]: recover from main branch after primitives are properly factored...

end Continuity.Codec.Dhall
